<h1>Listing private_orders</h1>

<table>
  <tr>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @private_orders.each do |private_order| %>
  <tr>
    <td><%= link_to 'Show', private_order %></td>
    <td><%= link_to 'Edit', edit_private_order_path(private_order) %></td>
    <td><%= link_to 'Destroy', private_order, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Private order', new_private_order_path %>
